# Required
TELEGRAM_BOT_TOKEN=
GEMINI_API_KEY=
ADMIN_PASSWORD=

# Optional: allow the bot to stay running even if secrets are missing
# ALLOW_EMPTY_SECRETS=1

# Optional: group chat IDs (format: -1001234567890 or -1001234567890/4 for topic)
GROUP_1_CHAT_ID=
GROUP_2_CHAT_ID=
GROUP_3_CHAT_ID=
GROUP_4_CHAT_ID=
GROUP_5_CHAT_ID=

# Optional: SheetMaster (database) integration
# If you start both via `make run` (docker compose): http://backend-go:8080
# If bot runs locally (non-docker): http://localhost:8080
# If bot runs in Docker but SheetMaster is exposed on host: http://host.docker.internal:8080
# SHEETMASTER_API_BASE_URL=http://backend-go:8080
# SHEETMASTER_API_KEY=sk_...
# SHEETMASTER_CATALOG_FILE_ID=1

# Optional: Realtime bridge for SheetMaster UI updates (when bot changes DB)
# If bot runs locally: http://localhost:4000/api/internal
# If bot runs in Docker and SheetMaster is on host: http://host.docker.internal:4000/api/internal
# REALTIME_INTERNAL_URL=http://host.docker.internal:4000/api/internal
# REALTIME_INTERNAL_SECRET=converter-internal-dev-secret

# Optional: Postgres for orders
# - Docker Compose starts Postgres automatically; you can leave this empty.
# - To use an external Postgres, set POSTGRES_DSN (example: postgres://user:pass@host:5432/db?sslmode=disable)
# POSTGRES_DSN=
# - Or override local Postgres credentials used by docker-compose.yml:
# POSTGRES_USER=upg_user
# POSTGRES_PASSWORD=1
# POSTGRES_DB=upg_db

# Optional: Admin credentials for auto-creating the database if missing
# (Only used if the DB does not exist and normal user cannot create it.)
# POSTGRES_ADMIN_USER=postgres
# POSTGRES_ADMIN_PASSWORD=
# POSTGRES_ADMIN_HOST=postgres
# POSTGRES_ADMIN_PORT=5432
# POSTGRES_ADMIN_DB=postgres
# POSTGRES_ADMIN_DSN=postgres://postgres:password@postgres:5432/postgres?sslmode=disable
