# Database Configuration
DB_DSN=host=localhost user=user password=password dbname=converter_db port=5439 sslmode=disable
DB_HOST=localhost
DB_USER=user
DB_PASSWORD=password
DB_NAME=converter_db
DB_PORT=5439
DB_ADMIN_USER=postgres
DB_ADMIN_PASSWORD=
DB_ADMIN_HOST=localhost
DB_ADMIN_PORT=5439
DB_ADMIN_DB=postgres
DB_ADMIN_DSN=

# Database connection pool settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME_MINUTES=5

# JWT Configuration
# CRITICAL: Change this to a strong random secret in production!
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_IN_PRODUCTION

# Server Configuration
PORT=8080
SERVER_PORT=8080

# CORS Configuration (comma-separated origins, no spaces)
ALLOWED_ORIGINS=

# File Upload Configuration
MAX_UPLOAD_SIZE_MB=10

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60

# Email Configuration (for verification and password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@yourapp.com
APP_URL=http://localhost:8080
